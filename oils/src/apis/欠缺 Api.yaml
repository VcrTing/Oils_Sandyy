API:
  單個產品的詳情頁面:
    1.
      User 購買產品 的 api  # 用戶購買後，他的 LP 點數、賬戶余額、PV 點數 等應當產生變動

    2.
      Aadmin 修改 「購買後獲得的 LP 點數」 的 api

    3.
      單個產品的 庫存 變更記錄 的 api   # 要能夠 根據 加貨減貨 等操作 進行過濾
      
  產品庫存變更記錄:
    4.
      記錄庫存更改 的 api # 含 備註
    5.
      Admin 新增庫存記錄 的 api

  增加LP積分:
    6.
      記錄 LP 積分 增減 的 api # 含 備註

    7.
      Admin 能操作 User 的 LP 積分 的 api

  更改會員等級:
    8.
      記錄 會員等級變動 的 api # 含 備註

    9.
      Admin 能 操作 會員的等級 的 api

  重啟會員資格:
    10.
      記錄 會員資格的重啟 記錄 的 api # 含 備註
    
    11.
      Admin 能 啟用 會員的資格 的 api

額外參數:
  用戶忠誠度積分:
    1. 會員的等級 參數

    2. 忠誠度積分操作記錄 的 備註
